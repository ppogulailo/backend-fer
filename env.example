# Copy to .env and set values for docker compose
# cp env.example .env

POSTGRES_USER=app
POSTGRES_PASSWORD=changeme
POSTGRES_DB=appdb

JWT_SECRET=change-me-in-production

PORT=4000
FRONTEND_ORIGIN=http://localhost:3000
